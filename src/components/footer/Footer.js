import { Container } from '@nextui-org/react';
import React from 'react';

const Footer = () => {
  return (
    <Container css={{ margin: '9em 0 2em 0', textAlign: 'center' }}>
      <small>App desarrollada sin fines comerciales</small>
    </Container>
  );
};

export default Footer;
